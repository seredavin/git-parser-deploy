apiVersion: v1
kind: Service
metadata:
  name: ui-service
  namespace: git-parser
  labels:
    app: hello
spec:
  type: ClusterIP
  selector:
    app: git-parser-ui
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP